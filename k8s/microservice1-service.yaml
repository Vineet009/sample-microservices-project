# microservice1-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: microservice1
  namespace: ns-vs-demo
spec:
  selector:
    app: microservice1
  ports:
  - protocol: TCP
    port: 8081
    targetPort: 8081